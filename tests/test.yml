---
- hosts: localhost
  vars:
    openstack_version: "liberty"
    openstack_controller_host: localhost
    openstack_compute_node_ip: 10.0.0.31
    openstack_controller_ip: 10.0.0.11

  roles:
    - ansible-role-nova-compute-node